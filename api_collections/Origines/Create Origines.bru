meta {
  name: Create Origines
  type: http
  seq: 2
}

post {
  url: http://localhost:4000/api/api/origines
  body: json
  auth: inherit
}

body:json {
  {
    "libelle": "La présidence de la République"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
